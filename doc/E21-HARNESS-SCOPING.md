# E2.1 Four-Arm Harness — Scoping Map (2026-06-12)

> Provenance: generated by a read-only scout agent over this repository at
> `490c4b6` (= public `v0.1.0`), 2026-06-12. Design input for the four-arm
> extension of the pilot. Pilot numbers below are transcribed from `results/`.

## The four arms

1. **KB+ME four-layer** — typed routing to knowledge-base (supersession) and memory-engine (bi-temporal), as in the pilot.
2. **Flat single store** — SQLite+FTS5 control, as in the pilot.
3. **Flat + four per-type persistence-policy profiles** — NEW: same flat schema, per-FactType decay/persistence policies. This is the falsification-register control: if it matches arm 1 within CI, the thesis restates as _persistence policies, not architecture_.
4. **Learned router** — NEW: cheap-model classifier replacing the oracle (fit on oracle labels, evaluate on held-out split).

Scale-up: all 10 BEAM categories (currently 2) × 100K split → 400 questions (currently 80).

## What exists (reusable)

| Abstraction                                                                       | Where                                                   | Reuse                                                 |
| --------------------------------------------------------------------------------- | ------------------------------------------------------- | ----------------------------------------------------- |
| Duck-typed store interface (`search`/`ingest_batch`/`retrieve_*`)                 | `src/baseline/store.py`, `src/routing/{kb,me}_store.py` | Formalize as Protocol; arms become store instances    |
| Evaluation loop (load → ingest → classify → retrieve → generate → score → report) | `src/experiment.py:29-207`                              | Parameterize store list; loop over arms               |
| Oracle + heuristic routers                                                        | `src/classifier/router.py:49,58`                        | Oracle stays as ceiling reference; heuristic as floor |
| Scoring (0-4 rubric, hard caps) + significance (bootstrap 10K, McNemar)           | `src/evaluation/llm.py`, `src/analysis/significance.py` | Unchanged across arms                                 |
| Ingestion heuristics (claim detection, negation-overlap contradictions)           | `src/routing/ingest.py:69,131`                          | Generalize to per-type policies                       |
| ME CLI accumulate/flush pattern                                                   | `src/routing/me_store.py:30-111`                        | Template for policy-store ingest                      |

## Gap list (blockers first)

| Gap                                          | Where                           | Action                                                                                                                                 |
| -------------------------------------------- | ------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| `EXPERIMENT_CATEGORIES` hardcoded to 2 of 10 | `src/dataset/types.py:26-31`    | Load all categories; per-category nulls pre-stated before runs                                                                         |
| No learned-router code (arm 4)               | absent                          | `src/classifier/learned_router.py`: features from question embedding, lightweight classifier fit on oracle labels, held-out evaluation |
| No per-type policy store (arm 3)             | absent                          | `src/baseline/policy_store.py`: flat schema + per-FactType decay/persistence at retrieval time                                         |
| Store paths hardcoded                        | `src/config.py:28-37`           | Arm-indexed path templating                                                                                                            |
| Experiment loop assumes 3 stores             | `src/experiment.py:62-95`       | Loop over arm list                                                                                                                     |
| Routing assumes KB/ME/flat dispatch          | `src/routing/router.py:145-166` | Parameterized retrieval per arm                                                                                                        |
| Contradiction detection heuristic-only       | `src/routing/ingest.py:69-168`  | Consider LLM-verified pass; quality risk for contradiction category                                                                    |

## Pilot reference numbers (oracle router, 100K split, N=80, 20 conversations)

| Category      | Typed | Flat  | Delta      | 95% CI           | McNemar p |
| ------------- | ----- | ----- | ---------- | ---------------- | --------- |
| Contradiction | 0.500 | 0.394 | +0.106     | [+0.019, +0.213] | 0.125     |
| Temporal      | 0.425 | 0.275 | +0.150     | [-0.006, +0.306] | 0.227     |
| **Overall**   | 0.463 | 0.334 | **+0.128** | [+0.038, +0.222] | 0.035     |

Heuristic keyword router (2-conversation subset): Δ=−0.125 — the reversal that motivates arm 4.

## Runtime dependencies

- LM Studio chat endpoint (generation + judge; pilot used the model recorded in `src/config.py`) — judge model must stay fixed across arms for score comparability.
- Ollama embeddings: `locusai/all-minilm-l6-v2` (384-dim).
- memory-engine release CLI binary; knowledge-base editable install.
- Tests: `PYTHONPATH=src uv run pytest` (57 passing at `490c4b6`). Smoke run ≈9 min (2 conversations); full pilot ≈90 min (20).

## Smoke findings (2026-06-13, two 2-conversation runs, WSL→localhost endpoints)

**Validated end-to-end:** BEAM load from HF cache, KB ingest (chunks + claims + supersession chains), flat ingest, oracle routing, LM Studio generation + judging (32 calls/run at `google/gemma-4-26b-a4b`), judge-error recovery, results writing. WSL mirrored networking serves both endpoints on `localhost`.

**Defects the four-arm harness must fix:**

1. **Silent arm death (critical).** ME batch-ingest failed in both runs; the runner logged `ME flush complete: 0 facts ingested` at INFO, completed all trials, wrote results, and exited 0. An arm-invalidating error must abort the run or stamp the output invalid — otherwise a dead arm masquerades as a measured Δ (run 2 "measured" typed=0.000 on temporal purely because ME was empty).
2. **Stale-DB handling.** `batch-ingest --create` refuses an existing `me.db` (run 1). Runner needs an explicit reset-or-resume policy per arm store; data dirs should be run-scoped.
3. **Results overwrite.** Runs write `results/{summary,trials}.json` in place, clobbering the committed pilot artifacts. Output must be run-stamped (`results/<run-id>/`). Smoke outputs preserved out-of-repo in `../smoke-2026-06-13/`; pilot `results/` + `data/` restored from pre-smoke backup.
4. **Judge fragility.** Run 1: one scoring call got HTTP 400 (caught, logged, recovered). Run 2: two `Could not parse judge output` → scored 0.00. The 1024-token judge budget vs gemma4's ~700 reasoning tokens is tight; raise the cap or constrain the response format before 400-trial runs.
5. **Embeddings touch the pilot only via ME CLI ingest** — KB/flat retrieval is FTS-only. Ollama availability is therefore an ME-arm-only dependency today.

**Open environment blocker (Windows side):** Ollama lists models but cannot start inference — `POST /api/embed` → HTTP 500 `llama-server binary not found` (searched `C:\Users\micha\AppData\Local\Programs\Ollama\lib\ollama\…`; server config shows `OLLAMA_VULKAN:true`, suggesting a source/custom build missing its built runner). ME ingest is blocked until this is fixed. Repro:

```bash
curl -X POST http://localhost:11434/api/embed \
  -d '{"model":"locusai/all-minilm-l6-v2","input":"x"}'   # → 500
```
